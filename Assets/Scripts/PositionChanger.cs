using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class PositionChanger : MonoBehaviour
{
    public Slider mass1XSlider;
    public Slider mass1YSlider;
    public Slider mass2XSlider;
    public Slider mass2YSlider;

    // Start is called before the first frame update
    void Start()
    {
        /*
        mass1XSlider.minValue = 0;
        mass1YSlider.maxValue = 0;
        mass1YSlider.minValue = 0;
        mass2XSlider.maxValue = 0;
        mass2XSlider.minValue = 0; 
        mass2YSlider.maxValue = 0;
        mass2YSlider.minValue = 0;
        mass2YSlider.maxValue = 0;

        mass1XSlider.onValueChanged.AddListener(ChangePosition);
        mass1YSlider.onValueChanged.AddListener(ChangePosition);
        mass2XSlider.onValueChanged.AddListener(ChangePosition);
        mass2YSlider.onValueChanged.AddListener(ChangePosition);
        
        */
    }
    void ChangePosition(int mass, string xory)
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
